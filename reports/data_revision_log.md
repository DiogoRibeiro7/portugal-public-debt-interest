# Data revision log

No previous processed vintage was available for comparison.

The processed directory is not version controlled, so the first build has nothing to diff against. This build has been archived; the next build will compare against it.
